'use client';

import { CaseDetail } from 'features/CaseDetail';

export const WidgetCaseDetail = () => {
    return (
        <section className="mb-8">
            <CaseDetail />
        </section>
    );
};
