'use client'

import { CaseListPublic } from 'features/CaseList';

export const WidgetCaseListPublic = () => {
    return (
        <section className="mb-8">
            <CaseListPublic />
        </section>
    );
};
