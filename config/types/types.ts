enum BuildMode {
    DEV = 'development',
    PROD = 'production',
}

enum BuildProject {
    FRONTEND = 'frontend',
    STORYBOOK = 'storybook',
    JEST = 'jest',
}

export {
    BuildMode,
    BuildProject,
}
