enum BuildMode {
    DEV = 'development',
    PROD = 'production',
}

enum ProjectMode {
    FRONTEND = 'frontend',
    STORYBOOK = 'storybook',
    JEST = 'jest',
}

export {
    BuildMode,
    ProjectMode,
}
